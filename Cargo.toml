[package]
name = "alice-grpc"
version = "1.0.0"
edition = "2021"
authors = ["Moroya Sakamoto"]
license = "MIT"
description = "Pure Rust gRPC framework with Protobuf encoding/decoding"

[lib]
crate-type = ["rlib"]
